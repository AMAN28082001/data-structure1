#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin>>T;
    while(T--){
        int N;
        cin>>N;
        int b[N],a[N];
        a[0]=b[0];

        for(int i=0;i<N;i++){
            if(b[i-1])
        }
        
    }
}